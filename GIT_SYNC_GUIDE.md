# 📝 Git 同步完整指南

## 完整的同步工作流程

### 步驟 1️⃣ ：查看當前狀態
```bash
git status
```
檢查有哪些文件被修改、未暫存的更改。

---

### 步驟 2️⃣ ：暫存（Stage）本地修改
```bash
# 暫存所有修改
git add .

# 或者只暫存特定文件
git add filename.py
```

---

### 步驟 3️⃣ ：提交（Commit）本地修改
```bash
# 提交修改並附帶信息
git commit -m "描述你的修改"

# 例如：
git commit -m "重構 scraper.py：提高效率和可維護性"
```

⚠️ **重點**：一定要先在本地提交，再同步遠程！

---

### 步驟 4️⃣ ：拉取（Pull）遠程更新
```bash
# 拉取遠程更新
git pull origin main
```

如果出現分叉提示，已配置好自動用 merge 合併。

---

### 步驟 5️⃣ ：如果有衝突（Conflict）

#### 5a️⃣ 查看衝突文件
```bash
git status  # 找到有 "both modified" 的文件
```

#### 5b️⃣ 解決衝突
編輯衝突文件，移除 `<<<<<<`、`======`、`>>>>>>` 標記，保留你要的內容。

#### 5c️⃣ 提交衝突解決
```bash
git add conflicted_file.md
git commit -m "Resolve merge conflict in conflicted_file.md"
```

---

### 步驟 6️⃣ ：推送（Push）到遠程
```bash
git push origin main
```

---

## 🎯 完整命令序列（一次性複製使用）

```bash
# 1. 查看狀態
git status

# 2. 暫存所有修改
git add .

# 3. 提交修改
git commit -m "Your commit message here"

# 4. 拉取遠程
git pull origin main

# 5. 推送到遠程
git push origin main
```

---

## ⚠️ 避免衝突的最佳實踐

### ✅ 推薦做法
1. **頻繁同步**：每天開始和結束時各同步一次
2. **小粒度提交**：每次修改一個功能就提交，不要累積大量修改
3. **同步前檢查**：同步前確保本地已提交
4. **避免修改auto-generate的文件**：如 `news.json`（應該用 `.gitignore` 排除）

### ❌ 容易出問題的做法
- ❌ 修改後不提交直接 pull
- ❌ 長時間不同步再一次性修改
- ❌ 同時修改 `news.json` 這類自動生成的文件

---

## 📦 建議的 .gitignore 設定

將 `news.json` 加入 `.gitignore`，因為它是由爬蟲自動生成的：

```bash
echo "news.json" >> .gitignore
git add .gitignore
git commit -m "Add news.json to gitignore"
git push origin main
```

之後修改 scraper.py 就不會有 `news.json` 的衝突了！

---

## 🆘 常見問題

### Q1: "fatal: Need to specify how to reconcile divergent branches"
```bash
git config pull.rebase false  # 已配置，無需重複設定
git pull origin main
```

### Q2: 想放棄本地修改，只用遠程版本
```bash
git reset --hard origin/main
```

### Q3: 想保留本地版本，丟棄遠程版本
```bash
git checkout --ours filename
git add filename
git commit -m "Keep local version"
git push origin main
```

### Q4: 查看提交歷史
```bash
git log --oneline -10  # 查看最近 10 個提交
```

---

